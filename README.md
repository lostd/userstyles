# Userstyles extension

This is a hack to manually inject CSS stylesheets to Chrome.

## Install

  - Go to chrome://extensions
  - Enable "Developer mode"
  - Click on "Load unpacked"
  - Select this directory and click open
